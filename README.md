# README

## Overview
This script runs `FINAL_CODE.py` with automatically calculated parameters.

It takes two arguments — start (`S`) and end (`E`) — and computes the number of tiles as `60*(E-S)/7.5`.

---

## Usage

### 1. Make the script executable
```bash
chmod +x run_final.sh
